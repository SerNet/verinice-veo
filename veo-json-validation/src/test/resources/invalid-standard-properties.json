{
  "definitions": {},
  "$schema": "http://verinice.com/veo/draft-01/schema#",
  "type": "object",
  "title": "Order-Schema",
  "description": "A sample schema to test that the standard properties, e.g id, type etc. are fixed.",
  "properties": {
    "id": {
      "type": "boolean",
      "title": "The UUID to identify the element"
    },
    "type": {
      "type": "number",
      "title": "The schema URI to identify the type of the element",
      "description": "The schema URI to identify the type of the element relative to the base URI",
      "enum": [
        "/schemas/order.json"
      ]
    },
    "title": {
      "type": "number",
      "title": "The title"
    }
  },
  "required": [
    "id"
  ]
}

{
  "definitions": {},
  "$schema": "http://verinice.com/veo/draft-01/link-schema#",
  "type": "object",
  "description": "Schema for Links between a process and an asset",
  "title": "process_asset",
  "properties": {
    "$veo.id": {
      "type": "string",
      "title": "The UUID to identify the element"
    },
    "$veo.type": {
      "description": "The name of the type described by this schema.",
      "enum": [
        "process_asset"
      ]
    },
    "$veo.title": {
      "type": "string",
      "title": "The title of a link"
    },
    "source": {
      "type": "string",
      "title": "id of the process"
    },
    "target": {
      "type": "string",
      "title": "id of the asset"
    }
  },
  "required": [
    "$veo.id",
    "$veo.type",
    "source",
    "target"
  ],
  "$veo.source.type": "process",
  "$veo.target.type": "asset"
}

{
  "definitions": {},
  "$schema": "http://verinice.com/veo/draft-01/link-schema#",
  "type": "object",
  "description": "Schema for Links between a scenario and an asset",
  "title": "scenario_asset",
  "properties": {
    "$veo.id": {
      "type": "string",
      "title": "The UUID to identify the element"
    },
    "$veo.type": {
      "description": "The name of the type described by this schema.",
      "enum": [
        "scenario_asset"
      ]
    },
    "$veo.title": {
      "type": "string",
      "title": "The title of a link"
    },
    "source": {
      "type": "string",
      "title": "id of the scenario"
    },
    "target": {
      "type": "string",
      "title": "id of the asset"
    }
  },
  "required": [
    "$veo.id",
    "$veo.type",
    "source",
    "target"
  ],
  "$veo.source.type": "scenario",
  "$veo.target.type": "asset"
}

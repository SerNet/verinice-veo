{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-06/schema#",
  "type": "object",
  "description": "Test JSON with non-ascii chars: äüöÄÜÖ€",
  "properties": {
    "id": {
      "type": "string",
      "title": "The UUID to identify the element",
      "description": "An explanation about the purpose of this instance."
    },
    "schema": {
      "type": "string",
      "title": "The schema URI to identify the type of the element",
      "description": "The schema URI to identify the type of the element relative to the base URI"
    },
    "parent": {
      "type": "string",
      "title": "The UUID to identify the parent",
      "description": "An explanation about the purpose of this instance."
    },
    "scope": {
      "type": "string",
      "title": "The UUID identifying the scope the element belongs to.",
      "description": "An explanation about the purpose of this instance."
    },
    "title": {
      "type": "string",
      "title": "The title of this element"
    }
  },
  "required": [
    "id",
    "title",
    "schema",
    "parent"
  ]
}

{
  "definitions": {},
  "$schema": "http://verinice.com/veo/draft-01/schema#",
  "type": "object",
  "description": "Schema for process groups from ISO 27000",
  "title": "ISO Asset Group",
  "properties": {
    "$veo.id": {
      "type": "string",
      "title": "The UUID to identify the element"
    },
    "$veo.type": {
      "description": "The name of the type described by this schema.",
      "enum": [
        "processgroup"
      ]
    },
    "$veo.title": {
      "type": "string",
      "title": "The title of the process group"
    },
    "parent": {
      "type": "string",
      "title": "The UUID to identify the parent"
    },
    "abbreviation": {
      "type": "string",
      "title": "The abbreviation of the process group"
    },
    "description": {
      "type": "string",
      "title": "The description of the process group"
    }
  },
  "required": [
    "$veo.id",
    "$veo.type",
    "$veo.title"
  ]
}

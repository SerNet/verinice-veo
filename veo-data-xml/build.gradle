plugins { id 'org.unbroken-dome.xjc' version '2.0.0' }

dependencies { compileOnly 'javax.xml.bind:jaxb-api' }

plugins.withId('eclipse'){
    eclipse {
        synchronizationTasks xjcGenerate
        autoBuildTasks xjcGenerate
    }
}
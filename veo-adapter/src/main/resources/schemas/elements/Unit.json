{
  "definitions": {
    
  },
  "$schema": "http://verinice.com/veo/draft-01/schema#",
  "type": "object",
  "description": "Schema for Unit",
  "title": "Unit",
  "properties": {
    "id": {
      "type": "string",
      "title": "The UUID to identify the element"
    },
    "type": {
      "description": "The name of the type described by this schema.",
      "enum": [
        "Unit"
      ]
    },
    "name": {
      "title": "name",
      "type": "string"
    },
    "abbreviation": {
      "title": "abbreviation",
      "type": "string"
    },
    "description": {
      "title": "description",
      "type": "string"
    },
    "client": {
      "type": "string",
      "description": "The id (UUID) of an element of the type client."
    },
    "units": {
      "type": "object",
      "description": "The contained objects of the type unit.",
      "properties": {
        
      }
    },
    "parent": {
      "type": "string",
      "description": "The id (UUID) of an element of the type unit."
    },
    "domains": {
      "type": "array",
      "description": "The ids (UUID) of elements of the type domain.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    }
  },
  "required": [
    "id",
    "type",
    "name",
    "client"
  ]
}

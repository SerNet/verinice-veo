{
  "definitions": {
    
  },
  "$schema": "http://verinice.com/veo/draft-01/schema#",
  "type": "object",
  "description": "Schema for Client",
  "title": "Client",
  "properties": {
    "id": {
      "type": "string",
      "title": "The UUID to identify the element"
    },
    "type": {
      "description": "The name of the type described by this schema.",
      "enum": [
        "Client"
      ]
    },
    "name": {
      "title": "name",
      "type": "string"
    },
    "units": {
      "type": "object",
      "description": "The contained objects of the type unit.",
      "properties": {
        
      }
    },
    "domains": {
      "type": "object",
      "description": "The contained objects of the type domain.",
      "properties": {
        
      }
    }
  },
  "required": [
    "id",
    "type"
  ]
}

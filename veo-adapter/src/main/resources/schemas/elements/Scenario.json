{
  "definitions": {
    
  },
  "$schema": "http://verinice.com/veo/draft-01/schema#",
  "type": "object",
  "description": "Schema for Scenario",
  "title": "Scenario",
  "properties": {
    "id": {
      "type": "string",
      "title": "The UUID to identify the element"
    },
    "type": {
      "description": "The name of the type described by this schema.",
      "enum": [
        "Scenario"
      ]
    },
    "name": {
      "title": "name",
      "type": "string"
    },
    "abbreviation": {
      "title": "abbreviation",
      "type": "string"
    },
    "description": {
      "title": "description",
      "type": "string"
    },
    "domains": {
      "type": "array",
      "description": "The ids (UUID) of elements of the type domain.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "links": {
      "type": "object",
      "description": "The contained objects of the type customLink.",
      "properties": {
        
      }
    },
    "customAspects": {
      "type": "object",
      "description": "The contained objects of the type customProperties.",
      "properties": {
        
      }
    },
    "owner": {
      "type": "string",
      "description": "The id (UUID) of an element of the type unit."
    },
    "riskvalueprovider": {
      "type": "object",
      "description": "The contained objects of the type riskDataValueProvider.",
      "properties": {
        
      }
    }
  },
  "required": [
    "id",
    "type",
    "name",
    "owner"
  ]
}
